# Configuration for Contrastive Divergence training
model:
  n_visible: 784
  n_hidden: 64
  model_type: rbm

training:
  epochs: 20
  learning_rate: 0.01
  batch_size: 64
  batch_limit: null
  optimizer: sgd
  checkpoint_every: 5
  checkpoint_path: rbm_cd_checkpoint.pth
  method: contrastive_divergence

cd_params:
  k_steps: 1  # CD-1 (standard CD)
  persistent: false
  use_momentum: false
  momentum: 0.5
  temperature: 1.0
  seed: 42

pm_params:
  gumbel_scale: 1.0
  solver_timeout: 60.0
  max_retries: 3
  seed: 42

data:
  dataset: mnist
  digit_filter: null
  image_size: [28, 28]
  data_root: ./data
  train_split: true
  download: true

solver:
  name: hexaly
  time_limit: 10.0
  suppress_output: false
  num_samples: 10
  relaxation_schedule: 1

inference:
  gibbs_steps: 1000
  num_generated_samples: 10
  reconstruction_samples: 5

logging:
  log_file: cd_training.log
  save_plots: true
  plot_every: 5
  figures_dir: ./figures